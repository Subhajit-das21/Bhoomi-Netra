/** Types for tokens.js — kept adjacent so `import ... from './tokens'` resolves. */

export declare const colors: {
  paper: string;
  'paper-deep': string;
  night: string;
  'night-soft': string;
  ink: string;
  'ink-soft': string;
  low: string;
  medium: string;
  high: string;
  critical: string;
  'high-wash': string;
  brand: string;
  olive: string;
};

export declare const radius: Record<'none' | 'sm' | 'md' | 'lg' | 'full', string>;

export declare const fontSize: Record<string, [string, string]>;
